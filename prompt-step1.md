你现在接手一个全新的项目：

Campus Delivery Desk（校驿）

项目根目录就是当前目录。

在开始任何编码之前，请首先完成以下工作。

一、读取项目规范

必须完整阅读：

- AGENTS.md
- docs/spec/README.md
- docs/spec/00_AI_EXECUTION_RULES.md
- docs/spec/ 下其余所有 Markdown 文档

这些文档共同构成本项目 V1 的权威需求规格。

这是一个全新的项目，没有历史版本，也不要假设存在任何旧实现。
不要把 docs/spec 中描述的“未来专业版技术栈”用于当前 V1。

当前 V1 必须使用文档指定的 Django 单体技术栈。

二、暂时不要编码

阅读完成后，先输出你的理解，包括：

1. 产品目标；
2. 用户角色；
3. 核心业务实体；
4. 各业务状态机；
5. 普通快递、代理单、归拢、费用项、结算、工资之间的关系；
6. V1 技术架构；
7. 推荐项目目录结构；
8. 根据 11_IMPLEMENTATION_PLAN.md 整理出的开发阶段；
9. 你发现的需求冲突、遗漏或者无法确定的问题。

不得因为觉得某个需求“不合理”而自行修改需求。

三、Git 检查

检查当前目录是否已经初始化 Git。

如果没有：

- 暂时不要直接创建远程仓库；
- 先确认 `.gitignore` 设计；
- 检查目录中不得包含密码、Token、数据库、真实客户信息、照片、日志、备份等内容。

同时检查：

git config user.name
git config user.email
gh auth status

如果 GitHub CLI 尚未认证，停止 GitHub 建仓步骤并明确告诉我需要我先运行 `gh auth login`。

四、输出结果

本轮只做：

- 文档理解；
- 架构审查；
- 开发阶段规划；
- Git 环境检查。

不要开始业务代码开发。

等待我确认后再进入 Phase 0。